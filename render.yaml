services:
  - type: web
    name: main-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000